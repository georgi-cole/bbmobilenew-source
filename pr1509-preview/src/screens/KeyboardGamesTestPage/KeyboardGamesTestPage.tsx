import MinigameLab from '../MinigameLab/MinigameLab'

/** A focused QA route for every minigame that requires text or numeric input. */
export default function KeyboardGamesTestPage() {
  return <MinigameLab keyboardGamesOnly />
}
